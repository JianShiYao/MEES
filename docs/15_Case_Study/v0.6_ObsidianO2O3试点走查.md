# v0.6 Obsidian O2/O3 真实接入试点走查（骨架）

> 走查编号：MEES-WT-004
> 版本：v0.6.0-dev
> 状态：**骨架，待真实证据**（连接真实 Vault 前所有结论为 `S`/待填）
> 所有者：配置管理员
> 最后更新：2026-07-15

## 1. 目的与边界

在**真实 Obsidian Vault** 上执行 O2 只读与 O3 受控写入试点，采集 [V6-G3/V6-G4 证据](../17_Tools/Obsidian与MCP接入策略.md)，关闭 `M6-01`/`M6-02`。**本文是待填骨架**：未连接真实服务前不得声称"已连接 Obsidian"；产品始终 `No-Go`；写入在 **V6-G4 人工批准前保持关闭**。

## 2. 前置条件（在你的机器上）

1. Obsidian 安装并启用社区插件 **Local REST API**，复制 API Key。
2. **REST 基址用去掉 `/mcp/` 的根**：`MEES_OBSIDIAN_URL=https://127.0.0.1:27124`（`/mcp/` 端点是给 MCP 客户端的，非本适配器）。设环境变量（仅 shell，勿提交）：`MEES_OBSIDIAN_URL`、`MEES_OBSIDIAN_TOKEN`。
3. **自签证书（A 方案，正常 TLS 校验）**：在插件设置下载证书，存为 `config/obsidian-cert.pem`（已 Git 忽略），把 `config/obsidian_integration.json` 的 `remote_ca_cert` 设为 `config/obsidian-cert.pem`。未配置证书时对自签 HTTPS 会按预期报 TLS 校验失败。
4. 确认状态：`python scripts/obsidian_adapter.py status` → `connected: true`。
5. 审计输出：`build/obsidian/audit.jsonl`（Git 忽略；只记 sha256 摘要，不记 token/正文）。安全：API Key 仅存环境变量；试点后建议在插件重新生成。

## 3. O2 只读试点（V6-G3）

> 环境：真实 Obsidian + Local REST API with MCP 插件，`https://127.0.0.1:27124`，CA 证书受信。2026-07-15 由 JianShi 在本机执行。审计源 `build/obsidian/audit.jsonl`（Git 忽略）。O2 读取源为 Obsidian 当前所开 vault（试点时为其"Embedded DevOps"知识库），非 MEES 仓库——插件只服务当前 vault，属预期。

| # | 证据项 | 命令 / 操作 | 结果 |
|---|---|---|---|
| 1 | 只读范围清单 | `status` 的 `read_roots` | ✅ `["docs","examples"]`；`connected: true` |
| 2 | 秘密检查 | `Select-String -Pattern "Bearer\|05b42" audit.jsonl` | ✅ 无命中——审计不含 token |
| 3 | 活动笔记读取样例 | `o2-active`（Obsidian 已打开笔记） | ✅ 返回实时活动笔记内容（内容不入库）；审计 `O2_ACTIVE/allowed`（sha256） |
| 4 | 按路径只读 | `o2-read docs/index.md` | ✅ `O2_READ/allowed` `sha256=fa6967…`（真实远端读取） |
| 5 | 断连降级测试 | 停用插件后 `o2-read docs/index.md` | ✅ `O2_READ/degraded_connection_error(URLError)` → `O0_READ/allowed` `sha256=b48441…`（回落本地 MEES），干净降级不崩溃；`o2-active` 断连记 `unavailable_connection_error` 并干净拒绝 |
| 6 | 脱敏日志 | `audit.jsonl` 摘录 | ✅ 仅 `action/target/outcome/sha256`，无正文无 token |

**试点发现并修复（真实 pilot 价值）**：首次断连时适配器崩溃（`ConnectionRefusedError`），未按策略 §5 降级。已修（提交 `fb84c92`）：连接级失败降级 O0 并审计，HTTP 错误仍暴露；活动笔记无 O0 兜底则干净拒绝。复跑证据见上 #5。

V6-G3 结论：**Go**（JianShi，2026-07-15）——真实 O2 只读接入（活动笔记/按路径读取、断连安全降级、脱敏日志）作为内部基线批准；不含写入（O3 仍受 V6-G4 控制），非真实独立评估，产品保持 `No-Go`。

## 4. O3 受控写入试点（V6-G4）

> **先决**：仅在 **V6-G4 人工批准后**，把 `config/obsidian_integration.json` 的 `local_write_enabled`（本地）或 `remote_write_enabled`（远端）置 `true`；写白名单保持 `examples/agent-drafts`。批准前以下命令应被拒绝（这本身是证据）。

| # | 证据项 | 命令 / 操作 | 结果（待填） |
|---|---|---|---|
| 1 | 路径白名单 | `status` 的 `write_roots` | [填写：范围] |
| 2 | 差异预览 | `python scripts/obsidian_adapter.py o3-propose examples/agent-drafts/demo.md --content-file <草稿>` → 打印 `proposal_id` 与 diff | [填写：proposal_id/diff] |
| 3 | 显式确认 | `python scripts/obsidian_adapter.py o3-apply <proposal_id> --confirm <proposal_id>` | [填写：已写入] |
| 4 | 成功写入 | 检查 `examples/agent-drafts/demo.md` 内容 | [填写：一致] |
| 5 | 拒绝越权 | `o3-propose docs/x.md --content "x"` → 应 `[rejected] path outside allowlist` | [填写：已拒绝] |
| 6 | 失败回滚 | `o3-rollback build/obsidian/rollback/<id>.json` → 恢复写前状态 | [填写：已回滚] |
| 7 | 审计日志 | `audit.jsonl` 中 `O3_PROPOSE/O3_WRITE/O3_ROLLBACK` 记录 | [填写：样例行] |

V6-G4 结论（待 JianShi）：[待填：Go / No-Go]

## 5. 走查结论

- **M6-01（V6-G3 只读）：证据齐备，建议关闭**——真实 Obsidian O2 连接完成活动笔记读取、按路径读取、断连安全降级和脱敏日志；试点并修复了断连崩溃缺陷（`fb84c92`）。待 JianShi 签 V6-G3 `Go`。
- **M6-02（V6-G4 写入）：仍 `No-Go`**——O3 写入未演练（`local_write_enabled` 仍为 `false`）；§4 待 V6-G4 批准开写后执行。
- 产品发布：保持 `No-Go`；Agent 输出仍 `human_review_required`，不批准任何门禁。
- 证据等级：真实连接读取为 `P`（对适配器与连接而言），不升级为产品或能力结论。
