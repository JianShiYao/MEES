# v0.6 基线评审记录

> 评审编号：MEES-REV-V06-002
> 版本：v0.6.0-dev
> 状态：人工评审候选（Conditional No-Go）
> 所有者：质量负责人
> 最后更新：2026-07-15

## 1. 评审范围

评审 WP0–WP7 的实现、自动验证、BMS 模拟走查和 Obsidian 安全边界，判断是否可提交 V6-G1–G6 人工决定。本记录由实现者整理，不构成独立质量复核。

## 2. 已完成建设

- WP0：治理、权限、证据状态和 O0–O3 边界已冻结。
- WP1：统一检查入口、三组双向追溯、历史 warning 关闭和 18 图真实渲染已实现。
- WP2–WP4：公共运行时、三份 Schema、六份协议和六类最小切片已实现。
- WP5：O0/O1、O2 REST 兼容只读与降级、O3 白名单/预览/确认/审计/回滚适配器已实现；真实服务门禁仍开放。
- WP6：MK8 六 Agent 机器走查完成，产品保持 `No-Go`。
- WP7：检查表和评审候选已形成；候选提交、Linux CI、干净导出和人工签署待完成。

## 3. 机器证据

| 证据 | 候选结果 |
|---|---|
| Python 自动测试 | 28 项通过，含合同、权限、降级、拒绝和精确回滚 |
| MK8 追溯 | 29 对象、52 关系、3 查询对、error=0、warning=0 |
| ESS 追溯 | 11 对象、19 关系、3 查询对、error=0、warning=0 |
| Mermaid | 18 个围栏真实渲染，0 失败 |
| BMS Agent 走查 | 6 类完成，全部需人工复核，发布 `No-Go` |
| MkDocs / 全量入口 | `run_all_checks` 11 项在 GitHub CI 全绿（run `29390873591`）；本地缺 `mmdc`，`mermaid_render` 仅 CI 可验 |

## 4. 评审发现

未发现严重问题。主要开放项如下：

| 编号 | 发现 | 责任人 | 关闭判据 |
|---|---|---|---|
| M6-01 | 缺少真实 Obsidian O2 只读连接、秘密脱敏和活动笔记读取证据 | 配置管理员 | **证据齐备（待 V6-G3 人工签署）**：2026-07-15 在真实 Obsidian（Local REST API with MCP，CA 证书受信）完成活动笔记读取、按路径读取、断连安全降级、脱敏日志（无 token）；见 [MEES-WT-004](../15_Case_Study/v0.6_ObsidianO2O3试点走查.md)。试点并修复断连崩溃缺陷 `fb84c92` |
| M6-02 | O3 仅在临时目录验证，未在批准白名单完成真实预览、确认、写入和回滚 | 配置管理员 / 工作产品所有者 | **证据齐备（待 V6-G4 人工签署）**：2026-07-15 在写白名单 `examples/agent-drafts` 完成预览、显式确认、写入、错误确认拒绝、越权拒绝、回滚与审计（见 [MEES-WT-004 §4](../15_Case_Study/v0.6_ObsidianO2O3试点走查.md)）；演练后 `local_write_enabled` 还原为 `false`。**范围**：受控本地文件写入机制；REST 写入 Obsidian 未实现，属后续能力 |
| M6-03 | 候选提交尚未经过 GitHub Linux CI 和干净导出复现 | 软件负责人 / 质量负责人 | **已关闭**：候选 `420b239` 推送后 GitHub Actions `mees-checks` run `29390873591` 绿色通过（全量 `run_all_checks`，含 `mermaid_render` 真实渲染）；`git archive` 干净导出复现 10/11 确定性检查（`mermaid_render` 需 CI 的 mmdc），归档 SHA-256 `c0b2146a0f91723150fe8d19da9a402346f4029d721af74534be8f0925e1a5aa` |

## 5. 门禁结论

- V6-G1（自动化输入可信）证据已就绪：F4/F5/F6 关闭，`run_all_checks` 统一输出在 GitHub CI 全绿（run `29390873591`），建议进入人工确认。
- V6-G2、V6-G5 建议进入人工确认。
- **V6-G3（Obsidian 只读）：`Go`**（JianShi，2026-07-15）——真实 O2 只读接入（活动笔记/按路径读取、断连安全降级、脱敏日志）批准为内部基线；见 [MEES-WT-004](../15_Case_Study/v0.6_ObsidianO2O3试点走查.md)。M6-01 关闭。
- **V6-G4（受控写入）：`Go`**（JianShi，2026-07-16）——受控本地写入机制（白名单/预览/确认/越权及错误确认拒绝/回滚/审计）批准为内部基线；**边界**：非 REST 写入 Obsidian，后续另评；写入默认关闭。M6-02 关闭。
- **M6-01、M6-02、M6-03 均已关闭**；V6-G6 当前 **Conditional No-Go**，剩余仅 **V6-G6 最终发布人工签署**。

## 6. 人工评审区

| 角色 | 人员 | 日期 | 结论 |
|---|---|---|---|
| V6-G1 自动化输入可信 | 待确认 | - | 证据就绪（F4/F5/F6 关闭、CI 全绿），未单独签署 |
| V6-G3 Obsidian 只读批准 | JianShi | 2026-07-15 | Go（真实 O2 只读接入内部基线；不含写入） |
| 工程过程负责人 | 待评审 | - | - |
| 质量负责人 | 待评审 | - | - |
| V6-G4 受控写入 | JianShi | 2026-07-16 | Go（受控本地写入机制；非 REST-to-Obsidian） |
| V6-G6 最终人工决定 | JianShi | - | 未决定 |

> 签署为仓库内部人工决定，非真实独立评估或认证。V6-G3 仅批准只读接入；O3 写入、产品发布另受 V6-G4/V6-G6 控制，产品保持 `No-Go`。
