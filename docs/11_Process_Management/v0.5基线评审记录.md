# v0.5 基线评审记录（V5-G6 收口）

> 评审编号：MEES-REV-V05-001
> 版本：v0.5.0-dev
> 状态：评审中
> 所有者：质量负责人
> 最后更新：2026-07-14

## 1. 评审目的

对 v0.5 自动化与度量候选（WP1–WP6 P0）执行 V5-G1–V5-G6 门禁评审，形成 v0.5.0 `Go / No-Go` 决策。只评价仓库内自动化与方法基线，不评价真实产品、组织能力或标准符合性。

## 2. 候选范围

| 项目 | 记录 |
|---|---|
| 输入基线 | `v0.4.0`（V4-G5 Go 内部模板基线） |
| 候选提交 | `aed845c`（未冻结为干净导出复现，见 F2） |
| 范围 | WP1 检查框架、WP2 追溯、WP3 指标、WP4 Dashboard+CI、WP5 目标级验证方法、WP6 ESS 方法试点 |
| 排除 | 真实目标/HIL/UL 9540A 执行、SIL 定量、独立 FSA、真实项目部署 |

## 3. 评审方式与独立性

本轮由构建这些切片的同一代理执行自评，**不是独立人员审核**（同前几版的模拟审计边界）。证据等级：工具可执行为 `P`（对工具而言），其处理的 MK8/ESS 数据为模拟 `S`；无真实项目 `P` 或独立评价 `I`。最终发布需真实独立评审人签署。

## 4. 门禁结论

| 门禁 | 结论 | 依据 |
|---|---|---|
| V5-G1 检查框架 | 有条件通过 | `check_mees` 规则/JSON/退出码就绪；跨平台一致性（F3）与脚本测试（F1）未完成 |
| V5-G2 追溯引擎 | 通过（内部） | 对象/关系、正反查询、断链/孤立/未定义诊断通过（MK8 + ESS） |
| V5-G3 指标合同 | 通过（内部） | 10 项指标口径 + N/A 规则冻结 |
| V5-G4 Dashboard/CI | 有条件通过 | Dashboard 与 CI workflow 建成、本地全绿；GitHub 实跑与跨平台稳定性未验证（F3） |
| V5-G5 方法试点 | 通过（内部，`S`） | MK8 目标级方法与 ESS 安全链模拟走查，缺证据正确阻断（No-Go） |
| **V5-G6 发布决定** | **Conditional Go — v0.5.0 标签 No-Go** | F1/F2/F3 未关闭；不满足"复现全部通过"的发布条件 |

## 5. 评审发现

### 5.1 严重问题
未发现。

### 5.2 主要问题（阻挡 v0.5.0 发布）

| 编号 | 发现 | 关闭判据 | 责任 |
|---|---|---|---|
| F1 | 自动化脚本无 committed 测试套件（M5 #11）；反虚假绿灯仅手工走查演示 | 为 check_mees/追溯/指标补成功、规则失败、无数据、格式错误、断链测试 | 软件负责人 |
| F2 | 未做干净导出复现，未冻结候选提交并回填哈希（M5 #12，前几版均已做） | `git archive` 候选提交到干净目录复跑全部检查与 Dashboard，回填 SHA-256 | 配置管理员 |

### 5.3 一般问题

| 编号 | 发现 | 处置 |
|---|---|---|
| F3 | 跨平台（Windows 本地 + GitHub Linux）一致性未验证（M5 #4） | CI 在 GitHub 实跑一次并比对本地结论 |
| F4 | 无单一本地"全检查"入口；三条双向查询未在单次显式产出（M5 #1、#5） | 增加聚合入口或文档化 CI 序列；扩展查询演示 |

## 6. 自动检查（本地 Windows，候选 `aed845c`）

| 检查 | 结果 |
|---|---|
| `check_mees`（LINK/NUM/MERMAID/EVID/NAV/LINK-002） | 102 受控 Markdown，error=0 |
| 本地 Markdown 链接 | 通过 |
| 模板资产 | 18 + 2 v0.5 草稿 + 9 试点 |
| 追溯（MK8 / ESS） | 均 error=0，未执行测试进入 `TRC-TEST-NOTRUN` |
| 指标 | 10 定义（5 算 / 5 N/A） |
| Dashboard / `mkdocs --strict` | 生成成功 / EXIT=0 |

> 以上仅本地 Windows；干净导出复现（F2）与 GitHub 实跑（F3）未完成，因此不作为发布级复现证据。

## 7. 人工确认（待 JianShi）

- [ ] 确认接受 WP1–WP6 P0 作为内部自动化与方法基线。
- [ ] 确认 v0.5.0 发布标签 No-Go，直到 F1/F2 关闭。
- [ ] 接受一般问题 F3/F4 的处置与目标。
- [ ] 确认证据边界：工具 `P`、数据 `S`、无真实项目/独立评价。

## 8. 决定

当前结论：**Conditional Go**。

- **允许**：WP1–WP6 P0 自动化与方法层作为内部基线继续使用与迭代；进入 F1/F2 收尾。
- **不允许**：创建 `v0.5.0` 发布标签、将 v0.5 声明为已发布、或对外声明自动化/度量已达发布级复现。
- **达成最终 Go 需**：关闭 F1（脚本测试）、F2（干净导出复现 + 冻结候选）、F3（跨平台），并由真实独立评审人签署。

## 9. 版本历史

| 版本 | 日期 | 修改人 | 修改说明 |
|---|---|---|---|
| v0.5.0-dev | 2026-07-14 | JianShi | V5-G6 收口：WP1–WP6 内部基线 Conditional Go，v0.5.0 标签 No-Go，登记 F1/F2 主要问题 |
