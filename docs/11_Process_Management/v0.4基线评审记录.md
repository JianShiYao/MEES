# v0.4 基线评审记录

> 评审编号：MEES-REV-V04-001
> 版本：v0.4.0-dev
> 状态：评审中
> 所有者：质量负责人
> 最后更新：2026-07-14

## 1. 评审目的

评价 v0.4 模板候选的范围、字段、唯一责任、标准差距追溯、复制性、项目实例边界和自动检查，形成 V4-G1–V4-G5 决策建议。

## 2. 候选范围

| 项目 | 内容 |
|---|---|
| 输入基线 | `v0.3.0` |
| 候选提交 | `337f288` |
| 模板范围 | 18 项：P0 13、P1 5 |
| 项目走查 | MK8 状态机模拟增量，9 项必须模板实例 |
| 排除范围 | 真实 MK8 项目批准、代码变更、构建/测试、产品安全/网络安全结论、标准符合性或认证 |

## 3. 评审方式与独立性

Codex 执行逐模板结构/内容审阅、自动检查和 MK8 模拟实例走查；该审阅不是独立人员审核。V4-G5 必须由 JianShi 人工确认，在确认前模板保持“评审中”，不得创建 `v0.4.0` 标签。

## 4. 门禁结论

| 门禁 | 候选结论 | 依据 |
|---|---|---|
| V4-G0 | 通过 | JianShi 批准范围、工作包、门禁和试点策略 |
| V4-G1 | 通过（内部候选） | 目录、元数据、占位符、复制、裁剪、角色和自动检查合同统一 |
| V4-G2 | 通过（内部候选） | 核心 G1–G6 模板完整并可复制 |
| V4-G3 | 通过（内部候选） | 功能安全、网络安全、供应方和 `EXT-HW` 模板完整，适用边界明确 |
| V4-G4 | 通过（模板可用性） | 9 项 MK8 实例完成，自动检查通过，缺证据发布被判为 `No-Go` |
| V4-G5 | `Conditional Go` 建议 | 模板严重/主要问题为 0；候选提交已复现，等待 JianShi 人工确认 |

## 5. 发现

### 5.1 严重和主要问题

模板层未发现开放严重或主要问题。

### 5.2 一般问题与保留差距

| 编号 | 发现 | 处置 |
|---|---|---|
| G1 | 系统/软件共用模板需要实例化时选择单一层级和最终责任 | 使用规则、模板说明和自动/人工检查共同约束 |
| G2 | 功能安全/网络安全专项模板未形成真实 MK8 实例 | 等产品边界和适用性确认后实例化；不阻断模板候选 |
| G3 | P1 模板没有真实项目数据 | 结构已完成，按风险/事件触发使用；保留 `CAP-001/005` 项目证据差距 |
| G4 | MK8 构建、测试、许可、SBOM 和 `EXT-HW` 证据缺失 | 项目 QA 实例登记，阻断产品 G4–G6，不阻断 M4 模板可复制结论 |

## 6. 自动检查

| 检查项 | 工作树结果 | `337f288` 干净 ZIP 导出结果 |
|---|---|---|
| 模板合同与复制实例 | 18 项模板，P0 13 / P1 5，MK8 实例 9，通过 | 同结果，通过 |
| 本地 Markdown 链接 | 145 / 145，通过 | 145 / 145，通过 |
| 正式文档编号 | 22 / 22 唯一 | 22 / 22 唯一 |
| 评审编号 | 3 / 3 唯一 | 3 / 3 唯一 |
| MkDocs | `build --strict` 通过 | `build --strict` 通过 |
| Git 差异 | `git diff --check` 与缓存区差异检查通过 | Git 归档直接来源于候选提交 |
| 归档校验 | 不适用 | SHA-256 `7C4E7A29D06BD570ADA6DBF75723F5E3A3988DF8A49D556ED88536D703D5E2E2` |

干净导出不包含原始 MK8 源码目录和外层仓库未受控内容；链接检查在导出目录初始化一次性 Git 索引后按同一脚本复现。

## 7. 人工确认

- [ ] JianShi 确认 18 项模板范围与唯一责任。
- [ ] JianShi 确认 MK8 实例为模拟 `S`，不构成产品批准或 `P/I` 证据。
- [ ] JianShi 确认模板层严重/主要问题为 0，并接受一般问题处置。
- [ ] JianShi 批准 V4-G5 和 v0.4.0 后续发布动作。

当前结论：**Conditional Go 建议。V4-G1–V4-G4 候选内容及提交级复现完成，仅等待 JianShi 人工评审；MK8 产品项目仍为 No-Go。**

## 8. 版本历史

| 版本 | 日期 | 修改人 | 修改说明 |
|---|---|---|---|
| v0.4.0-dev | 2026-07-14 | Codex | 冻结候选 `337f288`，回填模板、链接、编号、严格构建和 ZIP 干净导出复现结果 |
| v0.4.0-dev | 2026-07-14 | Codex | 建立 v0.4 候选评审记录，形成 V4-G1–V4-G4 内部通过与 V4-G5 Conditional Go 建议 |
